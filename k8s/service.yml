apiVersion: v1
kind: Service
metadata:
  name: devboard-frontend-service
  namespace: devboard
spec:
  selector:
    app: devboard-frontend
  ports:
    - port: 8081
      protocol: TCP
      targetPort: 4173
      NodePort: 30080
  type: NodePort
